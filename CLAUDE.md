# CLAUDE.md

Follow `AGENTS.md` for project-specific rules.

This project is a vanilla Chrome Extension MVP. Keep it dependency-free and biased toward selector safety. Update `DECISIONS.md` when scope, permissions, or architecture change. Working state lives in a local, untracked `STATUS.md`; create it if it is missing.
